import { Navbar, Sidebar } from '@/components/layout';

export const Home = () => {
    return (
        <div className="h-full">
            <Navbar />
            <Sidebar />
        </div>
    );
};
